<template>
  <div v-if="links.length > 3">
    <div class="flex flex-wrap mb-1">
      <template v-for="(link, k) in links" :key="k">
        <div v-if="link.url === null" class="mr-1 mb-1 px-4 py-3 text-sm leading-4 text-gray-400 border rounded"
          v-html="link.label" />
        <inertia-link v-else
          class="mr-1 mb-1 px-4 py-3 text-sm leading-4 border rounded hover:bg-white focus:border-indigo-500 hover:text-indigo-500 cursor-pointer"
          :class="{ 'bg-indigo-600 text-white': link.active }" :href="link.url" v-html="link.label" preserve-state />
      </template>
    </div>
  </div>
</template>

<script>
import { Link as InertiaLink } from '@inertiajs/vue3'
export default {
  components: {
    InertiaLink
  },
  props: {
    links: Array,
  },
}
</script>